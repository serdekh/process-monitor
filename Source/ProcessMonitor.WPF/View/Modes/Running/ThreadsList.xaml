<UserControl x:Class="ProcessMonitor.WPF.View.Modes.Running.ThreadsList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProcessMonitor.WPF.View.Modes.Running"
             xmlns:State="clr-namespace:ProcessMonitor.WPF.State"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20" Name="ThreadsListControl">
        <ListBox ItemsSource="{Binding Source={x:Static State:GlobalState.Instance}, Path=LatestSnapshotThreads}" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate 
                    d:DataType="SharedSnapshots:ThreadMetricsSnapshot" 
                    xmlns:SharedSnapshots="clr-namespace:ProcessMonitor.Shared.Snapshots;assembly=ProcessMonitor.Shared">
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding ThreadId}" FontWeight="Bold" Width="120"/>
                        <TextBlock Text="{Binding IsRunning}" FontWeight="Bold" Width="120"/>
                        <TextBlock Text="{Binding CpuTimeMs}" FontWeight="Bold" Width="120"/>
                        <TextBlock Text="{Binding IsAlive}" FontWeight="Bold" Width="120"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
